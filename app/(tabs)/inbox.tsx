import { Text, View } from "react-native";
import styles from "../styles";

export default function Inbox() {
    return (
        <View style={styles.mainContainer}>
            <Text style={styles.mainText}>Inbox screen</Text>
        </View>
    );
}
